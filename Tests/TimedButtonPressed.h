#include <iostream>
#include <iomanip>
#include <Windows.h>

// Wrote a basic function that waits for a key press for 5 seconds
// if the timer runs out before the 'F' key is pressed, the user
// is prompted with a try again message.
namespace TimedButtonPressed{

	void Main();
}